interface PortfolioCardProps {
    image: string;
    title: string;
    text: string;
    hypertext: string;
};

const PortfolioCard = ({image, title, text, hypertext }: PortfolioCardProps) => {
    return (
        <div>
            
        </div>
    );
};

export default PortfolioCard;